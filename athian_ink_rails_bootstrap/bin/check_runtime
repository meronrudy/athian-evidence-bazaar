#!/usr/bin/env bash
set -euo pipefail

# Runtime check for Ruby 3.3.12
actual="$(ruby -e 'print RUBY_VERSION')"
if [[ "$actual" != "3.3.12" ]]; then
  echo "ERROR: Expected Ruby 3.3.12, found ${actual}"
  exit 1
fi

echo "OK: Ruby version is 3.3.12"