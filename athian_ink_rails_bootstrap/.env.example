# Optional verifier fallback: path to a released INK verifier executable.
INK_VERIFIER_COMMAND=

# Preferred facade adapter: path to the Rust baink-cli executable.
# The local ink_receipts gem uses this when present.
INK_RECEIPTS_COMMAND=

# Required in production.
SECRET_KEY_BASE=
