<nav class="navbar navbar-expand-lg app-navbar sticky-top" aria-label="Primary navigation">
  <div class="container-fluid px-3 px-lg-4">
    <button class="btn btn-sm btn-outline-light d-lg-none me-2" type="button" data-bs-toggle="offcanvas" data-bs-target="#mobileSidebar" aria-controls="mobileSidebar" aria-label="Open navigation">
      <span aria-hidden="true">☰</span>
    </button>

    <%= link_to root_path, class: "navbar-brand d-flex align-items-center gap-2" do %>
      <span class="brand-mark">INK</span>
      <span>
        <strong>Evidence Bazaar</strong>
        <small class="d-block">Evidence first. Carbon second.</small>
      </span>
    <% end %>

    <div class="ms-auto d-flex align-items-center gap-2">
      <span class="badge rounded-pill text-bg-warning d-none d-md-inline">Pilot surface</span>
      <% if canonical_avsa %>
        <%= link_to "Open canonical chain", avsa_path(canonical_avsa), class: "btn btn-sm btn-light" %>
      <% end %>
    </div>
  </div>
</nav>
