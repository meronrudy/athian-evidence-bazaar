# AgEvidence Spec Examples

Use this directory for minimal schema fixtures that exercise the Rust `baink-agevidence` validator. The funded-startup end-to-end story lives in `examples/funded_startup`.

Recommended fixtures:

- valid model execution payload;
- valid country adapter commitment payload;
- valid country determination payload;
- invalid country determination without a parent reference;
- artifact profile payload with local verifier requirements.
