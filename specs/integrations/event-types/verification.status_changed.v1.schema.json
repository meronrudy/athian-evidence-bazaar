{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "athian.integrations.verification.status_changed.v1",
  "title": "verification.status_changed v1",
  "type": "object",
  "required": ["verification_engagement_id", "project_id", "verifier_id", "scope", "criteria", "criteria_version", "status", "exceptions", "effective_at"],
  "properties": {
    "verification_engagement_id": { "type": "string" },
    "project_id": { "type": "string" },
    "verifier_id": { "type": "string" },
    "scope": { "type": "string" },
    "criteria": { "type": "string" },
    "criteria_version": { "type": "string" },
    "status": { "type": "string" },
    "exceptions": { "type": "array", "items": { "type": "object" } },
    "statement_reference": { "type": "string" },
    "effective_at": { "type": "string", "format": "date-time" }
  },
  "additionalProperties": true
}
