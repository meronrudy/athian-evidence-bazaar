# Country Examples

Use the SDK fixture command for a local adapter evaluation:

```sh
agevidence fixtures run AU
agevidence fixtures run CA
agevidence fixtures run NZ
```

Project 4030 replay remains available through:

```sh
agevidence replay project-4030
```

